######################################
## .NET Core / C#
######################################
bin/
obj/
out/
[Log]*/
*.user
*.userosscache
*.sln.docstates

# User-specific files
.vs/
.vscode/
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates

# EF Core
*.db
*.db-shm
*.db-wal

# Secrets / Local Config
appsettings.Development.json
appsettings.Local.json
*.pfx
*.publishsettings

######################################
## Angular / Node / Frontend
######################################
node_modules/
dist/
.angular/
tmp/
.sass-cache/
connect.lock
coverage/
libpeerconnection.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Angular CLI generated files
.editorconfig
# Keep .editorconfig if you want to share it, but ignore
# IDE specific overrides if necessary.

# Environment-specific files
src/environments/environment.prod.ts
# Optional: if you use a local-only env file
src/environments/environment.local.ts

######################################
## Docker & OS
######################################
.dockerignore
# Don't ignore .dockerignore itself, but ignore its artifacts
*.tar
*.tar.gz

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

######################################
## Generated API (Orval)
######################################
# It is a best practice to ignore the generated client files
# IF your CI/CD generates them fresh using the v1.json.
# Remove these if you prefer to keep them in source control.
RestifyClient/src/api/generated/
RestifyClient/src/api/model/
RestifyServer/openapi/v1.json